const page = ({
    params
}: {
    params: {
        creatorId: string;
    }
}) => {

    console.log(params)
    return (
        <div>
            c id
        </div>
    );
}

export default page;